import { ImgAnnotatorDirective } from './img-annotator.directive';

describe('ImgAnnotatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgAnnotatorDirective();
    expect(directive).toBeTruthy();
  });
});
