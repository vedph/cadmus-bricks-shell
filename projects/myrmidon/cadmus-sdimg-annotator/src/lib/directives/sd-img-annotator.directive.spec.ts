import { SdImgAnnotatorDirective } from './sd-img-annotator.directive';

describe('SdImgAnnotatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SdImgAnnotatorDirective();
    expect(directive).toBeTruthy();
  });
});
